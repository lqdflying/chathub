import { UserImageConfig } from '@lobechat/types';

export const MIN_DEFAULT_IMAGE_NUM = 1;
export const MAX_DEFAULT_IMAGE_NUM = 20;
export const MIN_IMAGE_GENERATION_COUNT = 1;
export const MAX_IMAGE_GENERATION_COUNT = 50;

export const DEFAULT_IMAGE_CONFIG: UserImageConfig = {
  defaultImageNum: 4,
};
