---
description: "MCP tool arguments for microsoftdocs - use when calling any microsoftdocs tool"
alwaysApply: false
---

# MCP Tools: microsoftdocs

ALWAYS include `arguments` in CallMcpTool calls. NEVER send empty `{}`.

### microsoft_code_sample_search
Search for code snippets and examples in official Microsoft Learn documentation. This tool retrieves relevant code samples from Microsoft documentation pages providing developers with practical implementation examples and best practices for Microsoft/Azure products and services related coding tasks. This tool will help you use the **LATEST OFFICIAL** code snippets to empower coding capabilities.

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| `query` | string | yes |  | a descriptive query, SDK name, method name or code snippet related to Microsoft/Azure products, services, platforms, developer tools, frameworks, APIs or SDKs |
| `language` | string | no | `null` | Optional parameter specifying the programming language of code snippets to retrieve. Can significantly improve search quality if provided. Eligible values: csharp javascript typescript python powershell azurecli al sql java kusto cpp go rust ruby php |

Example:
CallMcpTool({ server: "microsoftdocs", toolName: "microsoft_code_sample_search", arguments: {"query": "search query"} })

### microsoft_docs_fetch
Fetch and convert a Microsoft Learn documentation webpage to markdown format. This tool retrieves the latest complete content of Microsoft documentation webpages including Azure, .NET, Microsoft 365, and other Microsoft technologies.

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| `url` | string | yes |  | URL of the Microsoft documentation page to read |

Example:
CallMcpTool({ server: "microsoftdocs", toolName: "microsoft_docs_fetch", arguments: {"url": "https://example.com"} })

### microsoft_docs_search
Search official Microsoft/Azure documentation to find the most relevant and trustworthy content for a user's query. This tool returns up to 10 high-quality content chunks (each max 500 tokens), extracted from Microsoft Learn and other official sources. Each result includes the article title, URL, and a self-contained content excerpt optimized for fast retrieval and reasoning. Always use this tool to quickly ground your answers in accurate, first-party Microsoft/Azure knowledge.

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| `query` | string | no | `null` | a query or topic about Microsoft/Azure products, services, platforms, developer tools, frameworks, or APIs |

Example:
CallMcpTool({ server: "microsoftdocs", toolName: "microsoft_docs_search", arguments: {"query": "search query"} })
