# Pinned so the converter behaviour matches what README.md documents.
markitdown[all]==0.1.7

fastapi==0.115.12
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Used only when MARKITDOWN_LLM_* is configured, for image captioning and — with
# the markitdown-ocr plugin — OCR of scanned pages.
openai==1.68.2
